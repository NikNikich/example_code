/*
export function handleStrings(types: any): string[] {
  if (!types) {
    return [];
  } else if (Object.prototype.toString.call(types) === '[object String]') {
    return [types];
  } else {
    return types;
  }
}
*/
